import product from './product'
import category from './category'
import order from './order'
import user from './user'
import banner from './banner'

export const schemaTypes = [product, category, order, user, banner]
